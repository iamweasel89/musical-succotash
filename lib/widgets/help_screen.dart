import 'package:flutter/material.dart';

// ── Мастерская: Справка ──────────────────────────────────────────────────────
// Живой словарь терминов и процессов. По мере роста приложения — растёт здесь.

class HelpScreen extends StatelessWidget {
  const HelpScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Справка')),
      body: ListView(
        padding: const EdgeInsets.all(16),
        children: const [
          _Section(title: 'Тезис', body: [
            'Краткая формулировка мысли, извлечённой из фрагмента текста. Не цитата — интерпретация.',
            '',
            'Создаётся из сообщения на канве: свайп по тексту выделяет фрагмент, кнопка ❝ на пузыре открывает режим «В тезисы». LLM формулирует тезис на основе фрагмента и контекста.',
          ]),
          _Section(title: 'Зачем нужны тезисы', body: [
            'Тезис — это промежуточное звено между сырым черновиком и готовым документом:',
            '• черновик — сырая запись, как оператор её сказал',
            '• тезис — вытащенная из черновика мысль в единичной формулировке',
            '• документ — синтез из многих тезисов',
            '• паспорт — документ, получивший канонический статус',
            '',
            'Разрезая длинные разговоры на тезисы, оператор превращает поток сознания в дискретные единицы, которыми можно оперировать: сортировать, группировать, отвечать, связывать.',
          ]),
          _Section(title: 'Как работать с тезисом', body: [
            '• **Формулировка** — LLM перепишет фрагмент как одну мысль (кнопка на карточке).',
            '• **Ответ** — кнопка ⚡ у поля «Ответ»: LLM отвечает на тезис.',
            '• **История вариантов** (в работе) — переключение между альтернативными формулировками тезиса.',
            '• **Источник** — тезис помнит, из какого пузыря чата он пришёл.',
          ]),
          _Section(title: 'Где живут тезисы', body: [
            'Все тезисы собираются в Мастерской → «Режим тезисов». Там — карточки с полями фрагмент / тезис / ответ. Оттуда их можно скопом выгрузить в инбокс (папка `documents/inbox/`) — markdown-файл с тезисами как материал для последующего LLM-разбора.',
          ]),
          _Section(title: 'Debug HTTP server', body: [
            'Включается в Мастерская → Отладка. Приложение поднимает локальный HTTP-сервер на порту 8080 (по умолчанию) и выдаёт состояние приложения в JSON + скриншот в PNG. Нужно для быстрой отладки через Claude Code.',
            '',
            'Endpoints: /state, /settings, /logs, /canvas, /theses, /decisions, /websearch, /ips, /screenshot.',
            '',
            'Доступ: через LAN (та же Wi-Fi) или через Tailscale (стабильный IP 100.x.x.x, работает вне домашней сети). Адреса смотри на экране «Отладка».',
            '',
            'Безопасность: API-ключи в /settings замаскированы, сервер принимает только GET. Выключается тем же переключателем.',
          ]),
          _Section(title: 'Связанные термины', body: [
            '**Канва** — гексагональная сетка нод (сообщения чата, тексты, API-вызовы), соединённых рёбрами. Чат — один из путей по канве.',
            '',
            '**Инбокс** — `documents/inbox/*.md`. Накопитель дампов и тезисов для последующего разбора. Просматривается из Мастерской.',
            '',
            '**Мастерская** — экран-хаб для инструментов и экспериментов. Открывается из настроек приложения.',
            '',
            '**Дамп** — ветка чата, сохранённая как markdown-файл в инбокс.',
          ]),
        ],
      ),
    );
  }
}

class _Section extends StatelessWidget {
  final String title;
  final List<String> body;

  const _Section({required this.title, required this.body});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.only(bottom: 20),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text(title,
              style:
                  const TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),
          const SizedBox(height: 8),
          ...body.map((line) => Padding(
                padding: const EdgeInsets.only(bottom: 2),
                child: SelectableText(line,
                    style: const TextStyle(fontSize: 14, height: 1.4)),
              )),
        ],
      ),
    );
  }
}
