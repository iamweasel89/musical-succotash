# Паспорт проекта — hex-canvas-mobile

**Репозиторий:** `iamweasel89/musical-succotash`
**Ветка разработки:** `claude/hex-canvas-mobile-08MeM`
**Последний коммит:** `ca01b1a`
**Стек:** Flutter (Android), Hive, path_provider, share_plus, file_picker

---

## Что приложение делает

Мобильный клиент с двумя режимами:
- **Канва** — гексагональная сетка, ноды (текст/API), рёбра, мультиканвас
- **Чат** — ветвящийся диалог с LLM, каждое сообщение = нода на канве

---

## Протокол работы

### Стиль общения
- Без вступлений, без резюме вопроса, без "конечно!".
- Оператор прыгает между темами — AI удерживает каждую как малый контекст.
- Краткость по умолчанию. Подробности — по запросу.

### Маркировка ответов
Каждый ответ AI начинается с номера: **1**, **2**...
Секции внутри: **12а**, **12б**. Оператор ссылается: "re: 12б".

### Маркировка задач
Задачи кодируются буквой группы + номером: Г1, Я2, Э3.
Оператор говорит "делай Я4" или "Я5 в закладки" — без расшифровки.

### Стопка
Список задач для следующего спринта. Формат:
```
**Группа — название**
- КОД  название — одна строка: что делает
- КОД ·★  название — срочное
```
Подробности раскрываются только по запросу "раскрой X".
Порядок выполнения не предлагается если не спросили.

### Ключевые слова
- **В закладки** — отложено, не потеряно, вернёмся
- **Стопка** — дай следующую партию задач
- **Строже** — критический разбор, не щадить
- **Черновик** — сырая мысль оператора, сохранить as-is
- **Паспорт** — канонический статус документа, не имя шага

### Протокол логов (отладка экстрактора)
- Повторяющаяся запись = оператор подтвердил баг (приоритет)
- Единственная запись = гипотеза AI (менее важно)

### Пуш
Часть конвейера. После готовой задачи: коммит → пуш → сообщи хэш → дальше.

---

## Архитектура

```
lib/
  models/
    app_model.dart       — AppModel (ChangeNotifier), canvases, nodes, edges
    node.dart            — Node (createdAt, updatedAt, type, status, text)
    canvas_data.dart     — CanvasData (createdAt, name, nodeIds, pan/zoom)
    edge.dart            — Edge (fromId, toId, waypoints)
    settings.dart        — GlobalSettings (API keys, toggles, showBubbleTime/Id)
  services/
    api_runner.dart      — callLlm(), streaming, multi-provider
    dump_service.dart    — DumpService.saveDump() → documents/inbox/*.md
  canvas/
    canvas_view.dart     — HexCanvas + switcher sheet
    hex_painter.dart     — CustomPainter (grid, nodes, edges)
    hex_math.dart        — hexToWorld, worldToHex
  widgets/
    excerpt_extractor.dart  — свайп-выделение слов, буфер, инерция
    settings_sheet.dart     — настройки (bottom sheet)
    api_node_sheet.dart     — настройки API-ноды
  chat_screen.dart       — ChatScreen, _ChatBubble, _ThesisEntry, _ThesisPanel
  main_screen.dart       — MainScreen (таб: чат / канва)
```

---

## Что реализовано

| Компонент | Статус |
|---|---|
| ExcerptExtractor — свайп-выделение, буфер, инерция, розовый фон | готово |
| Параграфный hysteresis в экстракторе (Э2) | готово |
| DumpService — дамп ветки в documents/inbox/ | готово |
| Режим тезисов — кнопка на пузыре, панель карточек, сохранение | черновик |
| CanvasData.createdAt | готово |
| Время + short ID на пузыре (опционально) | готово |
| Имя канваса + счётчик сообщений в тулбаре чата | готово |
| Изображения в чате (FileType.custom) | готово |
| Мультиканвас, pan/zoom, история (undo/redo) | готово |
| Markdown-рендеринг в пузырях | готово |

---

## Срочные задачи (раскрыть при обращении)

**Я — Тезисы (ядро)**
- Я1 ·★  LLM-формулировка тезиса — кнопка на карточке → ApiRunner → entry.thesis
- Я2 ·★  свайп на пузыре → тезис — механика экстрактора, if (markupActive)
- Я3  история вариантов тезиса — ◀▶ на карточке, макс 5, эфемерно
- Я4  редактируемый промпт формулировки — GlobalSettings, плейсхолдер {excerpt}
- Я5  свайп-выделение на пузыре (раскрытие Я2) — RenderParagraph hit-test
- Я6  контекст ±N слов вокруг excerpt — окно из source-ноды, теги
- Я7  LLM-ответ на тезис — вторая палочка у поля «Ответ»
- Я8  tap-to-source — стрелка на карточке скроллит к пузырю, подсветка

---

## Бэклог

**У — Управление тезисами**
- У1  копирование тезиса/ответа на карточке
- У2  метка на пузыре "уже в тезисах" + флаг в настройках
- У3  добавить целый пузырь как тезис без выделения

**О — Инбокс**
- О1  промпт для LLM-разбора черновиков (текст готов в Ключевых концепциях)
- О2  экран просмотра inbox/ внутри приложения
- О3  первый ручной прогон дерева концептов

**К — Конвейер**
- К1  режим источника — двойной вид (чат + канва), выбор нод/сообщений, кнопка "утвердить"
- К2  страница синтеза — "что делаем с кусочками", отдельная глава режима
- К3  синтез-нода — результат LLM-синтеза как нода особого типа в канве

**П — Паспорт как сущность**
- П1  действие "сделать паспортом" — повысить документ до канонического статуса
- П2  паспорт-нода в канве — особый тип, тело в локальном хранилище

**Р — Разделка дампов**
- Р1  обработка дампа в экстракторе — загрузить inbox-файл как источник текста
- Р2  черновики как отдельный экран — управляемый список с статусами

**Н — Итерация синтеза**
- Н1  повторный проход — взять готовый документ + новые фрагменты → ре-синтез
- Н2  ветвление тезисов — альтернативные формулировки (не только история Я3)

**З — Связи**
- З1  теги на документах/паспортах — Obsidian-стиль, поиск по тегам
- З2  тезис → нода канвы — тезис о конкретной ноде визуально к ней привязан
- З3  связи между тезисами — тезисы как граф, не список

**Б — База / поиск**
- Б1  поиск по инбоксу внутри приложения
- Б2  поиск по сообщениям в чате

**М — Мастерская как среда разработки**
- М1  перенести режим тезисов из чата в Мастерскую — тезисы как самостоятельное пространство
- М2 ·▲ Мастерская как встроенная среда разработки — Claude Code внутри приложения, контекст графа доступен AI, оператор и AI смотрят на одну канву

**А — Асинхронный режим**
- А1  обсудить архитектуру — состояние в артефакте, не в heads

**Ш — Защита системы** *(обсудить перед внедрением)*
- Ш1  пин/биометрия при запуске
- Ш2  шифрование хранилища — Hive AES
- Ш3  шифрование экспорта — JSON с паролем
- Ш4  блокировка канваса — read-only режим
- Ш5  блокировка ноды — поле locked
- Ш6  скрытый канвас — не виден без пина
- Ш7  API-ключи в Android Keystore
- Ш8  подтверждение деструктивных операций (удаление)
- Ш9  автоблокировка по таймауту
- Ш10  FLAG_SECURE — нет скриншотов и превью
- Ш11  автодамп при сворачивании/закрытии
- Ш12  глубокие снапшоты канваса — версионирование
- Ш13  проверка целостности импорта — checksum
- Ш14  ограничение экспорта по канвасу
- Ш15  двойное подтверждение clearAll — кодовое слово
- Ш16  маскировка содержимого нод в заблокированном состоянии

**Г — Граф (дальняя очередь)**
- Г1  обсудить: канва как порождающая структура — не отображение чата, а его источник
- Г2  обсудить: чат как частный случай канвы (линейный путь по графу)
- Г3  реализовать: перекрёстные рёбра между несмежными ветками
- Г4  реализовать: канва-узлы без чатового контекста (чистые идеи, не сообщения)

**В — Ввод**
- В1  голосовой ответ на тезис

**И — ID система** *(средний приоритет)*
- И1  4 символа base36 вместо 6 hex
- И2  псевдонимы нод (поле alias)
- И3  копирование ID по тапу
- И4  единый формат для нод, канвасов, тезисов

**С — Держать близко**
- С2  ID система — нужна спека
- С3  Меню ExpansionTile — ждёт накопления функционала

**Э — Экстрактор**
- Э1  выделение вверх — частично фикснуто через Э2, нужна проверка
- Э2  осцилляция абзацев — hysteresis добавлен, нужна проверка

**Д — Данные** *(дальние закладки)*
- Д1  updatedAt для CanvasData
- Д2  поле description у канваса

---

## Ключевые концепции

**Три уровня документа:**
черновик (сырая запись) → документ (синтез) → паспорт (канонический статус)

**Инбокс:** `documents/inbox/` — накопитель черновиков и тезисов для последующего LLM-разбора.

**Промпт разбора инбокса:**
> Вот записи оператора. Извлеки все концепты, функции, протоколы, решения.
> Для каждого: тип (функция/концепт/протокол/дизайн), статус (готово/черновик/очередь/обсудить/идея), одна строка. Выдай деревом.

**Режимы LLM-синтеза:** текст, JSON, outline/дерево, Q&A, теги, код, аннотация.

**Асинхронный режим:** оператор ведёт несколько тем параллельно без вступлений. AI держит каждую отдельно.

---

## Обновление паспорта

При завершении задачи или появлении нового концепта — обновить соответствующий раздел.
Коммитить вместе с кодом: `docs: update PASSPORT.md`.
