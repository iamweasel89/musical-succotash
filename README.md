# substrate

Параллельная ветка от [`claude/hex-canvas-mobile-08MeM`](https://github.com/iamweasel89/musical-succotash/tree/claude/hex-canvas-mobile-08MeM).

Статус: **экспериментальная**. Не трогает старую ветку. APK устанавливается как отдельное приложение (пакет `com.example.substrate`), не заменяет hex-canvas.

Компас: [`MOLECULE.md`](MOLECULE.md).

## Релизы

Тег-префикс: `sub-build-N`. APK-ассет: `substrate.apk`. Обновление — кнопка в приложении.
